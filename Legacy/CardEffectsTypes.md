니벨아레나의 카드 효과는 크게 **4가지 종류(기동, 자동, 지속, 트리거)**로 분류됩니다. 시뮬레이터 개발 시 이 분류는 효과가 발동되는 **‘타이밍’**과 **‘방식’**을 결정짓는 가장 중요한 메타데이터이므로, 반드시 카드를 파싱할 때 별도의 필드로 구분하여 엔진에 전달해야 합니다.
각 효과의 종류와 시뮬레이터 구현 시의 처리 방식을 정리해 드립니다.
1. 효과의 4가지 종류 (룰 8.1.3)
① 기동 효과 (Activated Effect)
• 정의: 턴 플레이어가 메인 페이즈나 어택 페이즈에 직접 선언하여 발동하는 효과입니다.
• 특징:
    ◦ 스킬 카드의 모든 효과는 기동 효과입니다.
    ◦ 유닛의 [액티브] 키워드도 기동 효과입니다.
    ◦ 비용(행동)을 지불할 수 없다면 발동할 수 없습니다.
② 자동 효과 (Automatic Effect)
• 정의: 특정 조건(이벤트)을 만족했을 때 자동으로 발동하는 효과입니다.
• 특징:
    ◦ [엔트리], [엑시트], [어태커], [디펜더], [이스케이프] 등이 여기에 해당합니다.
    ◦ 플레이어의 의사와 상관없이 조건이 맞으면 큐(Queue)에 등록됩니다(단, '할 수 있다'는 실행 시 선택).
③ 지속 효과 (Continuous Effect)
• 정의: 조건이 충족되는 동안 별도의 발동 선언 없이 상시 적용되는 효과입니다.
• 특징:
    ◦ [패시브], [암드], [전선구축], [믹스], [광전사] 등이 해당합니다.
    ◦ 스택이나 큐에 들어가지 않고, 게임 상태(State) 계산식에 즉시 반영됩니다.
④ 트리거 효과 (Trigger Effect)
• 정의: 대미지 처리 중 카드가 뒤집혔을 때 발동하는 특수 효과입니다.
• 특징: 카드 하단에 **[트리거 / ~]**로 표기된 효과입니다.
--------------------------------------------------------------------------------
2. 시뮬레이터 개발 시 데이터 구조화 방안
엔진이 효과를 올바르게 처리하려면, 카드 데이터를 로딩(Parsing)할 때 **EffectType**과 **TriggerCondition**을 명확히 구분해서 객체를 생성해야 합니다.
A. 데이터 모델링 예시 (JSON 구조 제안)
단순히 텍스트를 저장하는 것이 아니라, 엔진이 이해할 수 있는 Type으로 변환해야 합니다.
• 예시 1: 자동 효과 (엔트리)
• 예시 2: 기동 효과 (액티브/스킬)
• 예시 3: 지속 효과 (패시브)
B. 엔진에서의 처리 방식 차이
1. 자동 효과 (Event Listener 방식)
    ◦ 시뮬레이터에는 전역적인 **이벤트 버스(Event Bus)**가 필요합니다.
    ◦ 카드가 필드에 등장하면, 엔진은 해당 카드의 EffectType이 Automatic인지 확인하고, 맞다면 **"이벤트 리스너"**에 등록합니다.
    ◦ 예: Observer.Subscribe("OnAttackDeclared", card.HandleAttackEffect)
2. 기동 효과 (UI Command 방식)
    ◦ 이 효과들은 이벤트에 반응하는 것이 아니라, **유저의 입력(Input)**을 기다립니다.
    ◦ 카드를 클릭했을 때 EffectType이 Activated라면, "발동하시겠습니까?" 팝업을 띄우거나 코스트 지불 가능 여부를 체크하여 버튼을 활성화합니다.
3. 지속 효과 (Modifier Layer 방식)
    ◦ 이것은 발동(Trigger) 개념이 아니라 계산(Calculation) 개념입니다.
    ◦ 유닛의 파워를 표시할 때 basePower에 현재 활성화된 모든 Continuous 효과의 수치를 실시간으로 합산하여 보여줘야 합니다.
결론적으로: 시뮬레이터를 만드실 때, 카드 데이터베이스(DB)나 파서(Parser) 단계에서 텍스트를 분석하여 **"이 효과는 언제(Trigger), 어떤 방식(Type)으로 동작하는가?"**를 미리 구조화된 데이터로 정의해 두어야만 복잡한 체인 처리와 룰 구현이 가능합니다.