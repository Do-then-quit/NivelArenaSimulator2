# NivelArenaSimulator2 Agent Guide

## 0. 목적
- 이 문서는 새 채팅마다 에이전트가 빠르게 공통 컨텍스트를 복원하도록 만든 프로젝트 운영 가이드다.
- 구현/테스트/리팩터링은 항상 `NivelArena_Comprehensive_Rules_Ver.2.0.pdf` 기준으로 진행한다.
- 룰 해석이 애매하면 코드 관성보다 룰북 조항 번호를 우선한다.

## 1. 프로젝트 개요
- 프로젝트명: `NivelArenaSimulator2`
- 목적: 니벨아레나 TCG 규칙 기반 시뮬레이터 구현 (엔진 + UI + 카드/룰 회귀 테스트)
- 핵심 스택: TypeScript, Vite, Vitest
- 실행 명령:
  - 개발 서버: `npm run dev`
  - 빌드: `npm run build`
  - 전체 테스트: `npm test`
  - 단일/부분 테스트: `npx vitest run <파일경로>`

## 2. Source of Truth 우선순위
- 1순위: 카드 텍스트 (룰 1.3.1)
- 2순위: `NivelArena_Comprehensive_Rules_Ver.2.0.pdf` (최종 갱신일: 2026-01-19)
- 3순위: `tests/rules_v2_regression/`의 기대 동작
- 4순위: 현재 구현 코드 (`src/logic/`)
- 5순위: 보조 문서 (`GEMINI.md`, `knowledge.md`, `Legacy/`)

## 3. 룰북 Ver.2.0 핵심 요약

### 3.1 게임 기본
- 2인 대전 게임 (1.1.1)
- 패배 조건:
  - 대미지 존 10장 이상 (1.2.2.1, 1.2.2.2, 9.2.1.1, 9.2.1.2)
  - 대미지 처리 시점 덱 0장 (1.2.2.3, 9.2.1.3)
  - 드로우 실행 시점 덱 0장 (1.2.2.4, 9.2.1.4)
- 불가능한 행동은 가능한 만큼만 처리, 금지 효과가 지시 효과보다 우선 (1.3.2, 1.3.4)
- 유닛 파워 0이면 즉시 트래시 (1.3.7.3)

### 3.2 덱/준비
- 리더 1장 + 40장 덱 (5.1.2)
- 동일 식별 번호 최대 3장 (5.1.2.2)
- 트리거 카드 최대 8장 (5.1.2.3)
- 초기 5장 드로우 후 1회 멀리건 가능 (5.1.6)
- 선공 1턴 드로우 스킵 (6.3.1.1)

### 3.3 턴/페이즈
- 페이즈 순서: Level Up -> Draw -> Main -> Attack -> End (6.1.2)
- 메인 페이즈:
  - 카드 플레이(유닛 배치/아이템 장착/스킬 발동)
  - 기동 효과 발동 (6.4.1)
- 공격 페이즈:
  - 기동 효과 발동 + 공격 선언 (6.5.1)
- 엔드 페이즈:
  - [이 턴이 끝날 때] 처리 -> [~까지] 해제 -> 스킬존 트래시 -> 패 7장 조정 (6.6.1)

### 3.4 사이즈/코스트
- `사이즈 = 리더 레벨 + 대미지 존 카드 수` (4.7.2)
- 플레이 카드 코스트 + 필드 총 코스트 합이 사이즈 이하여야 함 (6.4.1.1.2)
- 업그레이드 시 기존 유닛/아이템 코스트는 새 합산에서 제외 (6.4.1.1.2.1)
- 각 유닛 존은 턴당 1회만 유닛 배치 가능(업그레이드 포함) (6.4.1.1.3)

### 3.5 전투
- 공격 선언 -> 방어 선언 -> 전투 -> 전투 종료 (7.2~7.5)
- 조우 유닛 없으면 히트만큼 직접 대미지 (7.4.2)
- 조우 유닛 있으면 파워 비교로 한쪽 트래시 (7.4.3)
- 공격 유닛이 전투 전에 영역 이동하면 전투 스텝 종료 (7.4.1)
- 어태커/디펜더 효과는 전투 종료 스텝에서 무효화 (7.5.1)

### 3.6 효과 처리(가장 중요)
- 효과 타입:
  - 기동 효과 (8.1.3.1)
  - 자동 효과 (8.1.3.2)
  - 지속 효과 (8.1.3.3)
  - 트리거 효과 (8.1.3.4)
- 자동 효과는 조건 만족 후 카드가 영역 이동했다면 발동 불가 (8.1.3.2.1)
- 동시 조건 만족 시 처리 순서: 턴플 지속 -> 상대 지속 -> 턴플 자동 -> 상대 자동 (8.1.3.2.3)
- 효과 처리 중 새 지속 효과 조건 충족 시 즉시 처리 (8.4.1)
- 동시 발생 효과 처리 도중 턴플 새 효과가 생겨도, 같은 배치의 상대 미처리 효과를 먼저 처리 (8.4.2)
- 대미지 처리 중 트리거 이외 효과는 대미지 처리 종료 후 발동 (8.4.3)
- 동일 행동 N회 지시는 동시 실행 취급 (8.4.4)
- 효과 처리와 무효 조건 동시 충족 시 무효 처리를 먼저 실행 (8.5.2.1)

### 3.7 키워드/서브키워드 우선 목록
- 키워드: 엔트리, 패시브, 액티브, 어태커, 디펜더, 엑시트, 암드, 전선구축, 레벨링크, 트리거, 믹스, 이스케이프, 체인, 버프
- 서브키워드:
  - 어태커 계열: 관통, 약탈, 돌파, 듀얼리스트, 침투
  - 디펜더 계열: 종결
  - 엑시트 계열: 공멸, 귀환
  - 기타: 각성, 광전사, 가디언(방벽/희생/상쇄), 크레딧

## 4. 코드베이스 맵
- 엔진 핵심:
  - `src/logic/types.ts`: 도메인 타입/enum, GameState 스키마
  - `src/logic/GameEngine.ts`: 턴/페이즈/전투/룰 처리 오케스트레이션
  - `src/logic/effects.ts`: EffectManager, 큐/지연큐/타겟/코스트 처리
  - `src/logic/effectActions.ts`: 액션 구현 레지스트리
  - `src/logic/RuleValidator.ts`: 플레이/공격/페이즈 종료 규칙 검증
  - `src/logic/TargetSelector.ts`: 타겟 계산
- 데이터/카드:
  - `packs/*.json`: 카드 데이터
  - `src/logic/cardEffects/*.ts`: 팩별/카드별 효과 정의
  - `src/logic/CardDatabase.ts`, `src/logic/CardParser.ts`: 데이터 로딩/변환
- UI:
  - `src/main.ts`, `src/SetupUI.ts`, `src/DeckBuilderUI.ts`, `src/HoverPreview.ts`, `src/TrashHoverOverlay.ts`
- 테스트:
  - `tests/rules_v2_regression/`: Ver.2 룰 회귀 핵심 게이트
  - `tests/cards/`: 팩 단위 통합 회귀
  - `tests/legacy/`: 과거 이슈/레거시 회귀
  - `tests/useful/`: 유틸/도메인/UI 보조 테스트
- 참고/아카이브:
  - `Legacy/`: 과거 문서/제안/아카이브 (현행 구현 근거로 직접 사용하지 말 것)

## 5. 구현 불변조건 (현재 엔진에서 특히 지켜야 할 것)
- 효과 큐 정렬: `creationTime` 오름차순 -> 턴 플레이어 우선
- `globalStep`을 효과 생성의 기준 시계로 사용
- 대미지 처리 중 비트리거 효과는 `deferredEffectQueue`로 지연
- 업그레이드로 트래시된 유닛은 엑시트 트리거가 발동하지 않아야 함
- 어태커/디펜더 기반 일시 효과는 턴 종료가 아니라 전투 종료에 해제
- 전투 상태(`combatStep`, `pendingAttackerIndex`, `phase`)는 전투 종료 후 정합하게 복구

## 6. 테스트 우선 개발 원칙
- 새 기능/버그 수정 전, 먼저 실패하는 테스트를 작성하거나 기존 회귀 테스트를 재현한다.
- 룰 변경/해석 반영 시 우선 추가할 위치:
  - 룰 시스템 변화: `tests/rules_v2_regression/`
  - 특정 카드 효과: `tests/cards/<pack>/`
  - 과거 버그 재발 방지: `tests/legacy/engine/` 또는 신규 회귀 파일
- 권장 검증 순서:
  - 1) 변경 지점 단위 테스트
  - 2) 관련 회귀 묶음
  - 3) `npm test` 전체
- 테스트 없이 룰/효과 동작을 변경하지 않는다.

## 7. 에이전틱 코딩 운영 규칙
- 작은 단위로 수정하고 바로 테스트한다.
- 룰북 조항 번호를 코드/테스트 이름/주석에 남겨 추적 가능하게 만든다.
- 무관한 리팩터링과 기능 수정을 한 PR/커밋에 섞지 않는다.
- 로그는 디버깅에 필요한 사건(효과 큐 삽입/소모, 페이즈 전환, 승패 판정)을 우선 남긴다.
- 의존성 업그레이드는 기능 목적이 명확할 때만 수행한다.
- 인코딩은 UTF-8을 기본으로 유지한다.

## 8. 장기 목표: 플레이 AI (강화학습/자가대전)
- 장기 방향: 니벨아레나를 플레이하는 AI 에이전트 학습 환경 구축
- 이를 위해 지금부터 유지해야 할 엔진 요구사항:
  - 결정론적 시뮬레이션: 시드 기반 RNG 주입 가능 구조
  - 명확한 행동 공간: 합법 액션 생성기와 액션 스키마 고정
  - 관측 가능 상태: 상태 직렬화/피처 추출 인터페이스 분리
  - 빠른 스텝 실행: UI 의존 없는 헤드리스 엔진 경로 유지
  - 재현성: 동일 seed + 동일 action sequence => 동일 결과
- 중기 실행 아이디어:
  - 1) Rule-complete self-play 환경 구축
  - 2) Heuristic/Baseline bot 추가
  - 3) Offline replay dataset 축적
  - 4) PPO 등 정책학습 실험 파이프라인 연결

## 9. 작업 시작 체크리스트 (에이전트용)
- 이번 변경이 참조하는 룰북 조항 번호를 먼저 확인했는가?
- 테스트를 먼저 만들거나 기존 실패 테스트를 재현했는가?
- `src/logic/` 변경 시 관련 회귀 테스트를 함께 돌렸는가?
- 동작 설명이 필요하면 테스트명으로 스펙이 읽히게 작성했는가?
- 결과가 룰북/테스트/코드 우선순위와 충돌하지 않는가?
